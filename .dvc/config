[core]
    no_scm = True
    remote = minio
['remote "minio"']
    url = s3://datasets
    endpointurl = http://localhost:9000
    access_key_id = minio
    secret_access_key = minio123
    region = us-east-1
